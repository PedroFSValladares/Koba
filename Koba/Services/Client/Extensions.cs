//using Discord.WebSocket;
using Koba.Services.Client;
using Koba.Services.Persistence;

namespace Microsoft.Extensions.DependencyInjection {
    public static class Extensions {
        /*
        public static DiscordClient InitWithoutConfig(this IServiceProvider services, string botToken) {
            return new DiscordClient(botToken, services.GetRequiredService<IDataBaseService>());
        }
        public static DiscordClient ConfigureClient(this IServiceProvider services,
            DiscordSocketConfig clintConfig, string botToken) {
            return new DiscordClient(botToken);
        }
        */
    }
}
